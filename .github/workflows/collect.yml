name: Collect A-share market data

on:
  push:
    branches: [main]
    paths:
      - "collector_cloud.py"
      - "config.json"
      - "requirements.txt"
      - "public/data/wencai_sector_overrides.json"
      - ".github/workflows/collect.yml"
  workflow_dispatch:
    inputs:
      refresh_history:
        description: Refresh BaoStock technical history
        required: false
        default: true
        type: boolean
      refresh_sectors:
        description: Refresh full-market industry mapping
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "26 1 * * 1-5"
    - cron: "36 1 * * 1-5"
    - cron: "32 2 * * 1-5"
    - cron: "32 5 * * 1-5"
    - cron: "47 6 * * 1-5"
    - cron: "12 7 * * 1-5"

permissions:
  contents: write

concurrency:
  group: market-data-collector
  cancel-in-progress: true

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Collect and score
        id: collect
        continue-on-error: true
        timeout-minutes: 20
        env:
          FINNHUB_API_KEY: ${{ secrets.FINNHUB_API_KEY }}
        run: |
          ARGS=""
          if [ "${{ github.event.inputs.refresh_history }}" = "true" ]; then ARGS="$ARGS --refresh-history"; fi
          if [ "${{ github.event.inputs.refresh_sectors }}" = "true" ]; then ARGS="$ARGS --refresh-sectors"; fi
          python collector_cloud.py $ARGS
      - name: Publish latest snapshots
        if: always()
        run: |
          git config user.name "ai-trading-data-bot"
          git config user.email "actions@users.noreply.github.com"
          git add public/data
          if git diff --cached --quiet; then
            echo "No data changes"
          else
            git commit -m "data: refresh market snapshot"
            git push
          fi
      - name: Mark unusable collection as failed
        if: steps.collect.outcome == 'failure'
        run: |
          echo "No fresh full-market snapshot was collected. Previous valid data was preserved."
          exit 1
